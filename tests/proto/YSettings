def known_flavors():
    return ['debug', 'release']

def get_common_config(config, args):
    return {
        'go_module': 'foo.com',
        'proto_gen_dir': 'ybtproto',
        'cache_salt': '1.0',
    }
